interface Product {
  id: number;
  name: string;
  cost: number;
  color: string;
}

interface user {
  name: string;
  age: number;
  city: string;
  hobby: string;
}

export type { Product, user };
